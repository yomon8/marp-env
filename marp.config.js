module.exports = {
    // スライドのタイトル
    inputDir: './slides',

    // テーマファイルを指定（必要に応じて）
    themeSet: './themes',
    // HTMLタグを有効にする
    html: true,

    // Mermaidを有効にする
    mermaid: true,

    // MathJaxも有効にする（数式表示のため）
    math: 'mathjax',

    // その他の設定
    allowLocalFiles: true, // ローカルファイルへのアクセスを許可
}
